/**
 * Application route paths used in E2E tests.
 * Centralized route definitions for consistency across tests.
 * @constant
 */
export const routes = {
  signIn: "/sign-in",
  home: "/",
  feedback: "/feedback",
} as const;
